/**
 * スプレッドシートを「ウェブに公開」した CSV の URL を貼ってください。
 * タブごとに公開 URL が違います。空欄なら data/*.json を使います。
 *
 * ■ キャスト
 *   名前, フリガナ, 性別, 種族, 学年, 性格, 誕生日, イメージカラー, 一言,
 *   アイコン, 写真1, 写真2, 写真3, 紹介動画, X, 表示
 *   アイコンは一覧用（16:9・横長）。写真1〜3は詳細の全身写真（9:16・縦長、最大3枚で切り替え）。
 *   紹介動画・X は URL を入れるとリンク表示（新しいタブで開く）。空欄なら出ません。
 *
 * ■ お知らせ
 *   日付, カテゴリ, タイトル, URL
 *
 * ■ ロゴ
 *   Git の assets/images/logo.png を使用（スプレッドシート不要）
 *
 * ■ スライド（トップの流れる写真）
 *   画像, タイトル
 *
 * ■ ギャラリー
 *   画像, 題名
 */
window.SITE_CONFIG = {
    castsCsvUrl: "",
    newsCsvUrl: "",
    slidesCsvUrl: "",
    galleryCsvUrl: ""
};
